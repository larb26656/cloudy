{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/cli.js",
  "bin": {
    "cloudy": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "copy-assets": "tsx ../../scripts/copy-assets.ts",
    "dev": "tsx watch src/cli.ts serve --config=config --dataDir=config/data --host=127.0.0.1",
    "dev:ui": "tsx watch src/cli.ts serve --config=config --dataDir=config/data --host=127.0.0.1 --ui --ui-dir ../../apps/web-app/dist",
    "start": "node dist/cli.js",
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/server": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22.15.3",
    "commander": "^14.0.0",
    "eslint": "^9.39.1",
    "picocolors": "^1.1.1",
    "tsup": "^8.0.0",
    "tsx": "^4.21.0",
    "typescript": "5.9.2"
  }
}
